# d=576 Standard MoE smoke test — 100 steps, throughput measurement only
# Run to estimate B2 full-run time before committing to paper run.
# 100 steps x 8 accum x 4 batch x 1024 seq = 3,276,800 tokens
model_dim: 576
n_heads_prelude: 9
n_heads_recurrent: 9
n_heads_coda: 9
mla_latent_dim: 144
max_seq_len: 1024
prelude_layers: 6
coda_layers: 1
max_loop_iters: 8
total_steps: 100
warmup_steps: 10
batch_size: 4
grad_accum_steps: 8
checkpoint_every: 100
eval_every: 100
hardware: rtx3090
run_name: dev_576/smoke_standard_moe
# MoE routing — matches B2 paper config
n_experts: 16
n_shared: 2
top_k: 2
expert_dim: 256
moe_aux_loss_coef: 0.01
